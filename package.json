{
  "name": "sui-registry",
  "version": "0.3.0",
  "description": "sui Registry",
  "scripts": {
    "change-commit": "pnpm run build:registry && git status",
    "build:registry": "node ./scripts/build-registry.js",
    "bump": "pnpm changeset version && node ./scripts/sync-version.js"
  },
  "keywords": [],
  "author": "Nayan",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
